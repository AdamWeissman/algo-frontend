import { combineReducers } from 'redux';

// actual reducer Import here (one for whichMode and one for allAlgorithms... and another for algosWithExamples)
// actual reducer Import here etc (reducers should be in their own file)... 


//export below as root reducer
//make line 9-12 its own constant, and then export it (possibly)
export default combineReducers ({
  placeholder: () => 'go away error message' 
});